filters:
  and:
    - type == "daily"
    - file.folder != "templates"
views:
  - type: table
    name: Recent dailies
    order:
      - file.name
      - date
      - tags
    sort:
      - property: date
        direction: DESC
