filters:
  and:
    - type == "meeting"
    - file.folder != "templates"
views:
  - type: table
    name: All meetings
    order:
      - file.name
      - date
      - attendees
      - project
      - status
    sort:
      - property: date
        direction: DESC
